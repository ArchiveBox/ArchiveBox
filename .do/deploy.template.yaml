spec:
  services:
    - name: archivebox
      image:
        registry_type: DOCKER_HUB
        registry: docker.io
        repository: archivebox/archivebox
        tag: latest
      http_port: 8000
      instance_count: 1
